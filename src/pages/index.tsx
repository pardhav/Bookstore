import { Box } from "@chakra-ui/react";
import Layout from "components/Layout";
import type { NextPage } from "next";

const Home: NextPage = () => {
  return <Layout>
    <Box>
      this is home
    </Box>
  </Layout>;
};

export default Home;
